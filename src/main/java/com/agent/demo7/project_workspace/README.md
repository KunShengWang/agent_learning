# Java Sample Project Workspace

This workspace is the small Java project operated by the lesson 7 coding agent demo.

Suggested tasks:

- Find `greetUser` and add friendlier empty-name handling.
- Change `DEFAULT_THEME` from `light` to `dark`.
- Locate discount logic and summarize how it works.

Compile this sample project from the workspace root:

```powershell
mvn -q compile
```
